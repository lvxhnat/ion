/**
 * Finds the peaks in a given time series
 * @param arr The array containing the prices/time series
 * @param peaks The number of peaks to find in the time series
 * Algorithm References:
 * - https://stackoverflow.com/questions/22583391/peak-signal-detection-in-realtime-timeseries-data/22640362#22640362
 */
export function findPeaks(arr: number[], peaks: number = 5) {}
