import { ENDPOINTS } from 'common/constant/endpoints';

export const REQUEST_ENDPOINTS = {
    CURRENT_WEATHER: {
        NAME: 'Current Weather',
        ENDPOINT: ENDPOINTS.PRIVATE.CURRENT_WEATHER_ENDPOINT,
    },
};
