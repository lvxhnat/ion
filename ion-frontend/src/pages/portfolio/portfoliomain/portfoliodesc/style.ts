import { ColorsEnum } from 'common/theme';

import { styled } from '@mui/system';


export const PortfolioDescMetadataBody = styled('div')(({ theme }) => ({
    width: "100%",
    display: "flex",
    flexDirection: 'column',
    alignItems: 'flex-end',
    justifyContent: "flex-end",
    flexGrow: 1,
}));
